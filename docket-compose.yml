version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: retize_user
      POSTGRES_PASSWORD: retize_password
      POSTGRES_DB: retize_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
